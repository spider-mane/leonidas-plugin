#!/usr/bin/env php
<?php

$project = 'leonidas/leonidas-plugin';

$root = dirname(__DIR__, 1);
$testDir = "$root/build/test-project";

$commands = [
  "rm -rf -f $testDir",
  "composer create-project $project $testDir --no-scripts"
];

shell_exec(implode(" && ", $commands));
